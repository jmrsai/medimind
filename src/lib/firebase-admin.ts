// All Firebase Admin SDK related code has been removed to prevent deployment errors.
// The application was attempting to use the Admin SDK without a valid service account,
// which is not suitable for a client-side or secure cloud environment without
// proper key management.

export const auth = null;
export const dbAdmin = null;
